# work

Per-destination scouting output lives in `work/<slug>/`:

- `<nn>-<name>.json` — stage outputs
- `packets/` — agent work packets
- `out/` — dry-run results

Everything else under `work/` is generated and git-ignored. This README is the only tracked file.
